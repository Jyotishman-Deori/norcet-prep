node_modules
dist
dist-admin
.DS_Store
.vercel
.env
.env.local
.env.*.local
*.log
.vite
dev-dist
# I18N font pipeline: downloaded Noto source TTFs + derived glyph lists
# (regenerable via scripts/build-locale-fonts.mjs; only the subsetted
# public/fonts/*.woff2 outputs are committed).
scripts/.font-src

.vercel
.env*
# ...but DO track the committed template (it holds only placeholders, no secrets).
!.env.example
