node_modules
dist
dist-admin
.DS_Store
.vercel
.env
.env.local
.env.*.local
*.log
.vite
dev-dist

.vercel
.env*
# ...but DO track the committed template (it holds only placeholders, no secrets).
!.env.example
